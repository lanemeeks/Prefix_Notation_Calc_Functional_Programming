2/25/26 10:12 am
I'm starting this initial respository while I'm at work so this session may go longer than neseccary if I have to pause. Trying to get a head start on this because I have lots of assignments, exams, and projects. There no time to dilly-dally. I've coded a prefix notation calculator before so the challenging part of this project will be to write it functionally. I've broken the project into chunks so that it's easier to approach. Today I hope to finish some imperative style pseudocode, next will be the actual coding. Broken up into smaller steps I need to code: user input functionality, prefix mathematical evaluation, error handling, and then general debugging/README file.
2/25/26 10:35am
This is the first working session and during it I hope to write some basic pseudocode of the program to guide me later. I hope to cover majority of the basic features of the program at a high level.
2/25/26 11:15am
That took me a minute to think about and write the pseudocode. I accomplished my goal for this session and finished the pseudocode. Next session I'll start on the user input, initialization, and overall structure of the program in functional programming. I an anticipate this project taking longer than I thought it would initially.
2/27/26 11:10am
Again working on this project while I'm at work. There's nothing new between sessions except for the fact that I'm glad I wrote the pseudocode. This is going to be a small session where I code the user functionality. What I'm going to do and my goal for this session is to write a recursive function acting as a user input loop, it should take the input and print it back to the user, unless the input is "quit" then it should exit.
2/27/26 11:32am
This part of the project was way easier than I thought it would be. I had to google input/output in racket because I haven't done it before but it wasn't complicated. I got what I intended to do done. I learned difference between display and displayln (adds newline duh), I also used flush-output (make sure output displays), and I learned how to store input using read-line and define. I made sure it's tail recursive as well, next session I think I'm going to code another recursive function that's called within the user-input function to evaluate the prefix statement.
3/2/26 4:14pm
This session will just be about the evaluate loop that evaluates the prefix statement that the input loop takes in. I will write it as a separate recursive function separate from the main program. I'm hoping that a stack-esque approach using cons, car, and cdr will work when taking the imperative implementation to the functional one. I might need to google stack alternatives in functional programming if my cons, car, and cdr idea does not work.
3/2/26 5pm
During this working session I decided to ignore the history implementation for now. My main issue was when I had to decide what to do with parenthesis, I was trying to parse them and it was getting overly complicated. Instead I realized I could use list? as a cond statement to parse it easier. I got the eval-exp function to work and I put it into the user-input function. There is currently no extensive input checking or validating other than checking if its a list or number. Next steps will be to implement the history of answers feature and I definitley accomplished my goal for this session. I know that the method is not tail-recursive, so another goal for next session is to make it tail recursive.
3/2/26 6:34pm 
My evening plans got canceled so I'm back! I'm going to work on making the eval-exp method tail-recursive and I decided I don't want to use eval to evaluate the operator because based on our security lectures, that's bad programming. Instead I'm going to hard code the mathematical operators. I explained mwhy my code was bad to one of my friends and it helped me understand how I can make the method tail-recursive by making it resemble the imperative version. If I take out the sub expressions and put them back in without parenthesis and then reverse the list I can evaluate it like a stack.
3/2/26 7:20pm
I was able to accomplish my goal for this session and write the function tail-recursively, the part I struggled the most with was setting up the recursive call at the end of the operator case. During the next session I'm going to implement the history of answers feature.
3/4/2026 2:30pm
Since the last sessions I've been thinking about types of input validation I'm going to need. During this session I'm going to implement the history of answers feature and start on input validation. I'm going to make sure I check that the user is using historical values that actually exist. This will include changes to the eval-exp method for sure.
3/4/2026 3:50pm
I was able to successfuly implement the history of answers features and I committed it during the session. At first my output was all icky and sometimes it wouldn't work and I realized I forgot to reverse the history list, whoops! Got that going and added history as a function parameter. Next I started on error checking, here are the errors I'm checking for: valid history references, the input having stuff it shouldn't have in it, checking for the right number of operands and operators, division by zero, bad operators, empty input, and general runtime errors. I learned about with-handlers and how it acts almost like a try catch statement in imperative programming so I added it in my user-input function hoping it would catch the bad stuff I didn't think to check for. However most types of errors just default to that error message in with-handlers so I might take it out and see if I'm ok without it next time, I might be using it wrong. I defintley accomplished my goal for this session and my project is so close to being done. Next session I might restruction the code to be more modular, fix the error messages to be more specific, and add the batch/interactive coding thing.
3/6/2026 11:38am
Guess who's at work working on this class project?! It's me! Ok, no new changes between sessions. During this session I'm going to try and get my error messages to be printed for their specific issue. The first thing I'm going to try is removing the with-handlers statement I've included as a catch all and see what happens. Next I'll implement the interactive/batch mode which basically means when the program is ran with the batch flag the only output is the results and errors nothing else.
3/6/2026 12:34pm
Ok, I was successful in achieving my goal for this session, I got rid of the with-handlers statement in user-input and that worked pretty well. I had a hard time handling errors like +2) is invalid but I added not one BUT TWO with-handlers to the top of eval-exp to check the start and end of the input for errors like that and it works. I also added the prompt? functionality, I was pretty thorough with making sure all my unecessary output checked for prompt? before printing. However, I'm having a hard time figuring out how to run it in my terminal so right now I'm just hoping that it works. Next session I'm going to finalize my code and update the readme file.
3/6/2026 3:54pm
I'm back to wrap this up, since the last session I was able to successfully run my program in batch mode from the terminal. The goal of this session is to write the README file and that's it, all done. Also kinda weird I'm looking at the changes made for this committ and it looks like I'll be pushing my final changes from the last session at the start of this session. I don't know what happened, I'm exhausted and I'm sorry.
